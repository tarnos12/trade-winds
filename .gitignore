# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Editor/IDE
.vscode/
.idea/

# Node (only if a dev/test toolchain is added later)
node_modules/
npm-debug.log*

# Local scratch / logs
*.log
/tmp/
scratch/

# Claude Code ephemeral subagent worktrees (agent-team model) — never commit
.claude/worktrees/

# Generated Artifact bundles (derived from tools/*.html for publishing) — not source
*.artifact.html
dist-desktop/
